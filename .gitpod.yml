image: mcr.microsoft.com/playwright/python:v1.42.0-focal

tasks:
  - name: Install dependencies
    init: pip install -r requirements.txt
    command: pytest

ports:
  - port: 9222
    onOpen: ignore

vscode:
  extensions:
    - ms-python.python
    - ms-playwright.playwright

